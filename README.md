A job application web project built using pure Node.js without Express framework.
The project includes a job page where users can fill out a job application form. Submitted data is stored in MongoDB using MongoClient, and full CRUD operations are implemented.

This project was developed to understand core Node.js concepts, including routing, request handling, and database operations without using Express.
